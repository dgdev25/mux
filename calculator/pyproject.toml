[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scientific-calculator"
version = "0.1.0"
description = "Safe scientific calculator CLI"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
